"""Packaged script support modules for runtime reuse.

This namespace currently exists so `scripts.shared` can be imported from
installed environments without relying on checkout-only `sys.path` mutation.
"""
