[project]
name = "pcce-example-repository"
version = "0.1.0"
description = "Credential-free fixture for the proof-context walkthrough"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src"]
testpaths = ["tests"]
