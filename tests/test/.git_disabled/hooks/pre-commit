#\!/bin/sh

# Run test generator validation
echo "🧪 Running test generator validation suite..."
python /home/barberb/ipfs_accelerate_py/test/.git/hooks/test_generator_test_suite.py
if [ $? -ne 0 ]; then
  exit 1
fi

# Continue with the commit if validation passes
exit 0
