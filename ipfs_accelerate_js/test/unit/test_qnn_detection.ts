/**
 * Converted from Python: test_qnn_detection.py
 * Conversion date: 2025-03-11 04:08:35
 * This file was automatically converted from Python to TypeScript.
 * Conversion fidelity might not be 100%, please manual review recommended.
 */

// WebGPU related imports
import { HardwareBackend } from "../hardware_abstraction";

#!/usr/bin/env python
"""
Test the QNN detection in the hardware_detection module.
"""

import * as $1
import * as $1
import ${$1} from "$1"

# Add the repository root to the Python path
sys.$1.push($2).parent))

from generators.hardware.hardware_detection import * as $1, HAS_ROCM, HAS_OPENVINO, HAS_MPS, HAS_QNN, HAS_WEBNN, HAS_WEBGPU
from hardware_detection.capabilities import * as $1, detect_qnn

$1($2) {
  """Test QNN detection"""
  console.log($1)
  console.log($1)
  console.log($1)
  console.log($1)
  console.log($1)
  console.log($1)
  console.log($1)
  console.log($1)
  
}
  console.log($1)
  qnn_results = detect_qnn()
  
  for key, value in Object.entries($1):
    if ($1) {  # Skip detailed info for brevity
    console.log($1)
  
    if ($1) {,
    console.log($1)
    if ($1) ${$1}\3")
      console.log($1)} MB")
      console.log($1))}\3"),
      console.log($1)}\3")
      console.log($1))} models"),
  } else {
    console.log($1)
    
  }
      return 0

if ($1) {
  sys.exit(main())