// FIXME: Complex template literal
/**;
 * Converted import { {HardwareBackend} from "src/model/transformers/index/index/index/index/index"; } from "Python: test_qnn_detection.py;"
 * Conversion date: 2025-03-11 04:08:35;
 * This file was automatically converted from Python to TypeScript.;
 * Conversion fidelity might not be 100%, please manual review recommended.;
 */;

// WebGPU related imports;";"

/** Test the QNN detection in the hardware_detection module. */;

import * as module; from "*";"
import * as module; from "*";"
import ${$1} from "./module/index/index/index/index/index";"
// Add the repository root to the Python path;
sys.$1.push($2).parent));

import { * as module, detect_qnn; } from "generators.hardware.hardware_detection import * as module, from "*"; HAS_ROCM: any, HAS_OPENVINO, HAS_MPS: any, HAS_QNN, HAS_WEBNN: any, HAS_WEBGPU;"
from hardware_detection.capabilities";"

$1($2) {/** Test QNN detection */;
  console.log($1);
  console.log($1);
  console.log($1);
  console.log($1);
  console.log($1);
  console.log($1);
  console.log($1);
  console.log($1)}
  console.log($1);
  qnn_results: any: any: any = detect_qnn();
  
  for ((key) { any, value in Object.entries($1) {) {
    if ($1) {  # Skip detailed info for brevity;
    console.log($1);
  
    if ($1) {,;
    console.log($1);
    if ($1) { ${$1}\3");"
      console.log($1)} MB");"
      console.log($1))}\3"),;"
      console.log($1)}\3");"
      console.log($1))} models");"
} else {console.log($1)}
      return 0;
;
if ($1) {;
  sys.exit(main());