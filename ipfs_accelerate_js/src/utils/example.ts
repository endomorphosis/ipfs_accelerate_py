/**
 * example.ts - Fixed placeholder implementation
 */

/**
 * Basic implementation for example
 */
export function example(options: any = {}): any {
  // Placeholder implementation
  return {
    execute: async (input: any) => {
      return Promise.resolve({ success: true });
    },
    dispose: () => {
      // Clean up
    }
  };
}
