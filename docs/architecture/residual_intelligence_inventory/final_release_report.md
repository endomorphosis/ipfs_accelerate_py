# Residual Intelligence final merged-tree report

This is a bounded, nonauthoritative observation. It cannot promote an expert,
accept a proof, or complete a task or goal.

## Lineage, receipts, and surface

- Start: commit `84a056e41e48a81d4484be43840196578d6c87da`, tree
  `40f0771e77d394ac91d92cc1edb02f7860f6131b`.
- End: task-start merged commit `3711bb4f5365ba469208891440076422686f5aa6`,
  tree `8fb5d6e7f1a747ee0338037afd4a3415d352e0d1`.
- The replacement report and its test are excluded from recursive
  self-qualification. The machine report has the complete 86-file scoped delta
  and the concrete public implementation-symbol catalogue.

Nine bounded producer receipts are digest-bound: baseline, prerequisite matrix,
corpus construction, PGIR gate, model-call inventory, frozen benchmark
manifest, synthetic admission, synthetic split manifest, and adversarial
campaign. They are observations, not proof, promotion, or completion receipts.

## Corpus, architecture, and experts

The only named admission is
`baguqeera3dp4lnpqc6wgksk5erxj54dt5sxhlp6opf2z5xyz6qvbg45abf2a`; it is
explicitly `training_unavailable`. Its public-fixture rights and split roots
are recorded in the machine report, along with a passing fixture leakage audit
(one group in each partition, zero cross-partition groups, and no hidden test
bodies accessed). That fixture is not an admitted production corpus.

There is no packaged expert, admitted learned tokenizer, candidate checkpoint,
or simulated checkpoint. Consequently there are zero registered experts and
zero counts in every closed expert disposition. No expert is routable.

## Metrics, costs, proof, and validation

The frozen benchmark population is 384 cases: 24 task families × four
partitions × four required case kinds. Both before and after preserve the full
384-case denominator; zero cases were executed on the end tree, so every
disposition and acceptance outcome is zero rather than omitted.

No current-tree paired benchmark, proof receipt, live GPU qualification,
promotion CAS, rollback CAS, or amortization measurement was run. Costs retain
all denominators: 384 frozen cases and zero local, remote, validation,
training, shadow, human-review, and rollback events. Break-even is
`not_applicable`; no cost or per-use saving was observed. The release-report
contract test is validation of this report shape only, not a proof or
qualification receipt.

## Eligibility, rollback, blockers, and nonclaims

Promotion eligibility is false. Every conjunctive hard gate is unsatisfied or
fixture-only; no aggregate score can compensate for missing rights, lineage,
privacy, safety, quality, efficiency, autonomy, or amortization evidence.
Drift is not observed because no expert is registered, and the required action
is to retain candidate-only behavior and requalify after admitted evidence.

There is no promoted route to restore. The exact rollback target is
`no-promoted-expert-route`: revoke this report root and regenerate from the
recorded tree if evidence is corrected. Never rewrite producer evidence or
promote from a report.

Blockers are `training_unavailable`, no admitted production corpus, no learned
tokenizer, no candidate checkpoint, a staged-but-unqualified benchmark, no
current-tree promotion evidence, and unavailable live GPU qualification.

Unsupported claims: `learned`, `verified`, `safe`, `autonomous`,
`token-efficient`, and `production-ready`.
