# Residual Intelligence final merged-tree report

This is a bounded, non-authoritative observation. It cannot create an
admission, checkpoint, expert, proof, promotion, rollback, or completion.

## Exact lineage and release surface

The qualified start is commit `84a056e41e48a81d4484be43840196578d6c87da`,
tree `40f0771e77d394ac91d92cc1edb02f7860f6131b`. The inspected final merged
snapshot is commit `d694b164cf196c2df48b45b494b3df4fdd3f3e87`, tree
`d121b4339de73a1a079c32c97b4c5a41f164e128`.

The snapshot is deliberately the tree before this report replacement: it does not recursively qualify its own replacement. The machine report binds the
three VRIF-032 output paths and its public symbols
`ResidualIntelligenceReleaseReport`, `ResidualGapReport`, and
`validate_release_claims`. It also binds SHA-256 blob inventories for the
VRIF-028 adversarial campaign, VRIF-029 control surface, VRIF-030 benchmark,
and VRIF-031 promotion/rollback producers.

## Corpus, splits, architecture, and experts

The sole admission is
`baguqeera3dp4lnpqc6wgksk5erxj54dt5sxhlp6opf2z5xyz6qvbg45abf2a`. Its corpus
root is `corpus:synthetic:vrif-fixtures-v1`, its source-rights root is
`baguqeerappawi4jtwvs3jfkoehdfc2xsh64lszuwjg6xzplgmckpg4ltjxda`, and its
split root is `baguqeeradbwccksw3vppbhz3xiu2l55isclxhi2xixthngpyeiy422jhmhzq`.
It remains `training_unavailable`; the public report exposes identities and
bounded dispositions only. Training, development, held-out, and adversarial
partitions remain named, and hidden test bodies were not accessed.

No architecture was selected, no learned tokenizer was admitted, no checkpoint
was created, and training was not attempted. Every one of the 24 task families
therefore has the explicit disposition `CAPABILITY_UNAVAILABLE`; no expert is
eligible or routable.

## Before/after denominators, costs, proofs, and drift

Before and after each retain all 96 frozen benchmark cases: four cases for each
of 24 families. Neither population was evaluated, so both record zero accepts
and 96 abstentions. Costs are exactly zero tokens and zero break-even because
no training, evaluation, remote call, validation, shadow run, human review, or
rollback event is being represented as a measured release cost.

The frozen benchmark identity, case root, binding set, paired-baseline identity,
and producer artifact bundle are machine-bound. The listed validation command
checks this report contract and board shape; it is not a proof receipt. Producer
and terminal Database Portal validations remain required. Drift was not run:
there is no admitted checkpoint, so no detector result exists.

## Eligibility, rollback, blockers, and unsupported gaps

Promotion eligibility is false. The rollback target is the exact starting
commit `84a056e41e48a81d4484be43840196578d6c87da`; any corrected observation
must revoke this report root and be regenerated from corrected evidence, never
rewrite evidence or promote from a report.

The blocker is `training_unavailable`. Not-run work is
`gpu_live_qualification`, `promotion`, and `training`. Unsupported claims are
`learned`, `verified`, `safe`, `autonomous`, `token-efficient`, and
`production-ready`.
