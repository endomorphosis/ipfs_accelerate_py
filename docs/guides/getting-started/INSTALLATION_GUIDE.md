# Installation Guide (Compatibility Path)

This filename is retained for compatibility with older links. The maintained
installation guide is [installation.md](installation.md), which is checked
against `pyproject.toml` and documents the current package name, extras,
CUDA paths, MCP setup, and validation commands.

Use the maintained guide rather than copying commands from the historical
sections that used to follow this notice.
