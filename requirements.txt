# Core runtime dependencies
anyio>=4.0.0
aiohttp>=3.8.1
duckdb>=0.7.0
ipfshttpclient>=0.8.0a2
websockets>=10.1

# Arrow (required by ipfs_kit_py cache/zero-copy features)
pyarrow>=14.0.0

# Always-installed ML runtime
torch>=2.1
transformers>=4.46

# Compatibility: ipfshttpclient may still depend on urllib3<2
urllib3<2

# HTTP + auth helpers
requests>=2.28.0
cryptography>=41.0.0

# MCP server + dashboard (used by the systemd service)
fastmcp>=0.1.0
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# GitHub integration (provides the `github` module)
PyGithub>=2.3.0

# Utilities
tqdm>=4.64.0
numpy>=1.23.0
toml>=0.10.2

# Proof-reuse/datasets Python surfaces.  These are ordinary Python
# distributions only: NLTK corpus downloads and the Groth16 Rust backend remain
# explicit first-use capabilities and never run as packaging side effects.
nltk>=3.8.1,<4
jsonschema>=4,<5

# Strict content-addressing (core).  Kept aligned with the proof-reuse extra
# and pyproject/setup metadata.  Datasets-ZK verification is *not* core: the
# proof-reuse lazy installer materializes a closed exact Git-blob snapshot in
# its owner-private content-addressed cache only when an exact lookup requests
# that capability. It runs no pip/VCS installer or package build hook (see
# requirements-proof-reuse.txt and [project.optional-dependencies].proof-reuse).
multiformats>=0.3,<1
pymultihash>=0.8.2

# ipfs_datasets_py is intentionally not a core dependency.  Its distribution
# depends back on ipfs_accelerate_py; the proof-reuse resolver materializes only
# the exact reviewed verifier snapshot without mutating global site-packages.
