# Core runtime dependencies
anyio>=4.0.0
aiohttp>=3.8.1
duckdb>=0.7.0
ipfshttpclient>=0.8.0a2
websockets>=10.1

# Arrow (required by ipfs_kit_py cache/zero-copy features)
pyarrow>=14.0.0

# Always-installed ML runtime
torch>=2.1
transformers>=4.46

# Keep the shared HTTP stack compatible with ipfs_datasets_py. The supported
# ipfshttpclient alpha accepts urllib3 2.x.
urllib3>=2.5.0,<3.0.0

# HTTP + auth helpers
requests>=2.28.0
cryptography>=41.0.0

# MCP server + dashboard (used by the systemd service)
fastmcp>=0.1.0
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# GitHub integration (provides the `github` module)
PyGithub>=2.3.0

# Proof-gated contract-repair Python toolchain.
# These are also available through the explicit first-use loader in
# ipfs_accelerate_py.agent_supervisor.integrations.contract_repair_dependencies.
# TypeScript is intentionally absent: it is an npm package and the same loader
# provisions the reviewed compiler API into a versioned user-local directory.
# The optional ErgoAI Java API Eclipse Temurin JDK is a reviewed external lazy
# dependency (never a mandatory pip package; managed via temurin-jdk).
z3-solver>=4.12.0,<5.0.0
cvc5==1.3.3
mypy>=1.8.0,<2.0.0
ruff>=0.12.0,<1.0.0

# Utilities
tqdm>=4.64.0
numpy>=1.23.0
toml>=0.10.2
multiformats>=0.3.0
pymultihash>=0.8.2
