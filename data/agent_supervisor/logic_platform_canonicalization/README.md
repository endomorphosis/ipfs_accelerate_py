# Logic platform canonicalization runtime

Sealed FormalWorkPlan and generated inventories live here. `state/`,
`worktrees/`, `merge-queue/`, and `logs/` are supervisor runtime and are not
sources of truth.
