# IPFS Accelerate Error Reporting Configuration
# 
# Copy this file to .env and configure your GitHub credentials
# to enable automatic error reporting to GitHub issues.

# GitHub Configuration
# --------------------
# Personal access token with 'repo' scope for creating issues
# Get one at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Repository in format 'owner/repo' where issues will be created
# Example: endomorphosis/ipfs_accelerate_py
GITHUB_REPO=your_username/your_repository

# Error Reporting Settings
# -------------------------
# Enable or disable error reporting (true/false)
ERROR_REPORTING_ENABLED=true

# Include system information in error reports (true/false)
ERROR_REPORTING_INCLUDE_SYSTEM_INFO=true

# Automatically add labels to issues (true/false)
ERROR_REPORTING_AUTO_LABEL=true

# Additional Configuration
# -------------------------
# Python logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable debug mode (true/false)
DEBUG=false

# P2P Cache Sharing (libp2p)
# -------------------------
# Enable P2P cache sharing between nodes (true/false)
CACHE_ENABLE_P2P=true

# libp2p listen port for the P2P cache (default: 9100)
CACHE_LISTEN_PORT=9100

# GitHub repo used for peer rendezvous / peer registry (format: owner/repo)
# Example: endomorphosis/ipfs_accelerate_py
IPFS_ACCELERATE_GITHUB_REPO=owner/repo

# GitHub auth token for gh (preferred). This enables the GitHub Issue-backed peer registry.
# You can also use GITHUB_TOKEN; GH CLI will use either GH_TOKEN or its authenticated login.
GH_TOKEN=your_github_token_here

# Optional: Some parts of the codebase still check GITHUB_TOKEN.
GITHUB_TOKEN=your_github_token_here

# Optional: Explicit shared secret for P2P message encryption (use the same value on every node).
# If not set, the system derives it from GH_TOKEN/GITHUB_TOKEN or `gh auth token`.
# CACHE_P2P_SHARED_SECRET=your_shared_secret
